name: Test

on:
  push:
    branches: [ "new_features_465031" ]
  workflow_dispatch:

permissions:
  contents: read

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Set up Python 3.10
      uses: actions/setup-python@v3
      with:
        python-version: "3.10"
    - name: Satisfy requirements
      run: |
        python -m pip install --upgrade pip
        pip install stem
    - name: Test rectangle
      run: python -m unittest rectangle.py || true
    - name: Test square
      run: python -m unittest square.py || true
    - name: Test circle
      run: python -m unittest circle.py || true
    - name: Test triangle
      run: python -m unittest triangle.py || true